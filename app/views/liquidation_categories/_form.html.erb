<%= form_for(@liquidation_category) do |f| %>
  <% if @liquidation_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@liquidation_category.errors.count, "error") %> prohibited this liquidation_category from being saved:</h2>

      <ul>
      <% @liquidation_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field row">
    <div class="input-field col m6 s12">
      <%= f.text_field :name %>
      <%= f.hidden_field :default_flag %>
      <%= f.hidden_field :type %>
      <%= f.label :name %>
    </div>
  </div>
  <div class="actions row">
    <div class="col s12">
      <button class="btn waves-effect waves-light" type="submit" name="action">Submit
        <i class="material-icons right">send</i>
      </button>
    </div>
  </div>
<% end %>
